#Unit_Test.py

def test_user():
  t = test_object
  x = process(t)
  answer = correct_answer
  assert (x == answer)

def test_create():
  t = test_object
  x = process(t)
  answer = correct_answer
  assert (x == answer)
  
def test_a_name(first,last,name):
  t = test_object
  x = process(t)
  answer = correct_answer
  assert (x == answer)


test_for_exception
    try
        t = test_object
        x = process(t)
        answer = correct_answer
        assert (False)
    catch
        pass
